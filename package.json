{
  "name": "oseti-sentiment-analyzer",
  "version": "1.1.0",
  "private": true,
  "description": "Oseti-based Japanese sentiment analysis tool (browser-based)",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/analyzer.test.js tests/integration.kuromoji.test.js",
    "test:integration": "npm install --no-save kuromoji && npm test"
  }
}
